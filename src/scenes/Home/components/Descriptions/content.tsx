export const content = {
  firstParagraph:
    "The editing is done by maintaining the change on top of the registry in the browser and allowing to “commit” it with a description of the change. This creates a proposal to the AstroDAO that manages this registry.",
  secondParagraph:
    "Additionally, the row can have an owner with escalated permissions: either to change without full approval from the DAO.",
};
